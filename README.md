# WOTAN

Esports/stats hub for EINHORN_INDUSTRIAL, being split out of `OKEMILY/tournaments.html` into its
own repo/subdomain. See `CLAUDE.md` for real, current status and related docs.

Not deployed yet — `index.html` is a real placeholder until `WOTAN-DNS-001` (Cloudflare subdomain)
and a real nginx server block land.
